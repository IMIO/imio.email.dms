[buildout]
extends =
    sources.cfg
    versions.cfg

show-picked-versions = true
allow-picked-versions = false

parts =
    scripts

versions = versions

eggs =
    imio.email.dms
    imio.email.parser
    email2pdf2

[scripts]
recipe = zc.recipe.egg:scripts
interpreter = runpy
eggs = ${buildout:eggs}
